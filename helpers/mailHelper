const nodemailer = require('nodemailer')
const smtpTransport = require('nodemailer-smtp-transport');


module.exports = { tTransporter: nodemailer.createTransport(smtpTransport({
  service: 'gmail',
  host: 'smtp.gmail.com',  
  auth: {
    user: 'PracaMailTestSLX',
    pass: 'ocqmmifopwzvteop'
  }
}))}
