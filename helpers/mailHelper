const nodemailer = require('nodemailer')

let config = {
  host: 'smtp.gmail.com',
  port:  587,
  secure: true,
  auth: {
    user: 'PracaMailTestSLX',
    pass: 'ajsgyifnxpiuglw'
  },
  tls: {
    rejectUnauthorized: false
  }
}

module.exports = { tTransporter: nodemailer.createTransport(config) }
